apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: prometheus
  name: flask-app
  namespace: flask-app
spec:
  endpoints:
  - port : http
    interval: 15s
    path: /metrics
    scrapeTimeout: 10s
  selector:
    matchLabels:
      app: flask-app